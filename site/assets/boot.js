/* Loaded synchronously in <head>. Marks the document as script-enabled so
   the reveal animations can hide content — without JS, nothing is hidden and
   the page renders fully. A landing page must never depend on JS to be read. */
document.documentElement.className += ' js';
